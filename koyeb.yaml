name: my-app
regions:
  - fra # Replace with your preferred region

build:
  commands:
    - apt-get update && apt-get install -y python3-pip
    - pip3 install -r requirements.txt

run:
  type: python
  cmd: "python app.py"
